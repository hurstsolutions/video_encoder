{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug decoder.exe",
      "type": "cppvsdbg",          // MSVC debugger
      "request": "launch",
      "program": "${workspaceFolder}/decoder.exe",
      "args": [],                  // add command-line args here
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "console": "integratedTerminal",
      "preLaunchTask": "build-decoder"
    }
  ]
}